--- mt-st-0.5b/stinit.c.jbj	Sat Apr 15 14:42:49 2000
+++ mt-st-0.5b/stinit.c	Sat Apr 15 14:43:42 2000
@@ -204,7 +204,7 @@
 	SKIP_WHITE(cp);
 	strcpy(bp, cp);
 	strcat(bp, " ");
-	len += strlen(lbuf) + 1;
+	len += strlen(cp) + 1;
 	if (fgets(lbuf, LINEMAX, dbf) == NULL)
 	    return FALSE;
 	if ((cp = strchr(lbuf, '#')) != NULL)
--- mt-st-0.5b/stinit.8.jbj	Sat Apr 15 14:43:53 2000
+++ mt-st-0.5b/stinit.8	Sat Apr 15 14:44:23 2000
@@ -119,7 +119,7 @@
 This keyword defines the string that must match the
 .B product identification
 returned by the device.
-.IP model=
+.IP revision=
 This keyword matched the string that must match the
 .B product revision level
 returned by the device.
